# Skill maintenance

- Never update the repository root `CHANGELOG.md` for changes limited to Agent Skills, skill metadata, plugin packaging, or skill documentation. Only change that changelog when the user explicitly requests it.
