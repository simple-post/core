interface:
  display_name: "SimplePost"
  short_description: "Publish and schedule social posts with SimplePost"
  brand_color: "#C6F432"
  default_prompt: "Use $simplepost to publish this content to my connected social accounts."

policy:
  allow_implicit_invocation: true
